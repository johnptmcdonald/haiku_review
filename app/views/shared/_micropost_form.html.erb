<%= form_for(@micropost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Compose new post..." %>

	<%= f.text_field :title, placeholder: "title", value: @title %>

	<%= f.text_field :picture, placeholder: "picture", value: @picture %>

	<%= f.text_field :year, placeholder: "year", value: @year %>

	<%= f.text_field :movie_ref, placeholder: "movie_ref", value: @movie_ref %>

  </div>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>